import { HexLiteralNode, } from '../ast/AST.js';
import AstTraversal from '../ast/AstTraversal.js';
export default class ContractAccessorTraversal extends AstTraversal {
    constructor(replacements) {
        super();
        this.replacements = replacements;
    }
    visitContractAccessor(node) {
        const key = `${node.contract.name}${node.accessor}`;
        if (this.replacements[key]) {
            return new HexLiteralNode(this.replacements[key]);
        }
        return node;
    }
}
//# sourceMappingURL=ContractAccessorTraversal.js.map