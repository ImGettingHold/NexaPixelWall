import { AbiFunction, Script } from '@nexscript/utils';
import { Utxo, Recipient, TransactionDetails, TokenDetails, Unlocker } from './interfaces.js';
import NetworkProvider from './network/NetworkProvider.js';
import SignatureTemplate from './SignatureTemplate.js';
export declare class Transaction {
    private address;
    private provider;
    private redeemScript;
    private unlocker;
    private abiFunction;
    private args;
    private templateScript;
    private constraintScript;
    private visibleArgs;
    private inputs;
    private outputs;
    private txid;
    private sequence;
    private locktime;
    private feePerByte;
    private hardcodedFee;
    private minChange;
    private tokenChange;
    constructor(address: string, provider: NetworkProvider, redeemScript: Script, unlocker: Unlocker, abiFunction: AbiFunction, args: (Uint8Array | SignatureTemplate)[], templateScript: Script, constraintScript: Script, visibleArgs: Script);
    from(input: Utxo): this;
    from(inputs: Utxo[]): this;
    fromP2PKT(input: Utxo, template: SignatureTemplate): this;
    fromP2PKT(inputs: Utxo[], template: SignatureTemplate): this;
    to(to: string, amount: bigint, token?: TokenDetails): this;
    to(outputs: Recipient[]): this;
    withOpReturn(chunks: string[]): this;
    withAge(age: number): this;
    withTime(time: number): this;
    withHardcodedFee(hardcodedFee: bigint): this;
    withFeePerByte(feePerByte: number): this;
    withMinChange(minChange: bigint): this;
    withoutChange(): this;
    withoutTokenChange(): this;
    build(): Promise<string>;
    send(): Promise<TransactionDetails>;
    send(raw: true): Promise<string>;
    private getTxDetails;
    meep(): Promise<string>;
    private setInputsAndOutputs;
}
